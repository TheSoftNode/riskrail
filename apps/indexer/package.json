{
  "name": "@riskrail/indexer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"indexer tests pending\"",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@riskrail/logger": "workspace:*",
    "@riskrail/stacks": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.20.5"
  }
}
